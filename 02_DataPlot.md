You can view the plot of any explanatory (X) variable below against the response (Y) variable, which is set to Salary.

Note that with categorical (factor) variables the R plot command renders box plots. 
